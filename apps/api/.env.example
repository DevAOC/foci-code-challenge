# Copy to .env and replace <macos-user> with your local username (run `whoami`).
# Local Homebrew Postgres trusts the OS user over localhost, so no password is needed.
DATABASE_URL="postgresql://<macos-user>@localhost:5432/foci_dev"
TEST_DATABASE_URL="postgresql://<macos-user>@localhost:5432/foci_test"
