# Design decisions

Each decision below records what was chosen, what was rejected, and why. They
came out of a structured design review before any code was written, and are
mirrored in [GitHub issue #1](https://github.com/DevAOC/foci-code-challenge/issues/1)
and [`plans/todo-persistence.md`](../plans/todo-persistence.md). Don't
re-litigate them without new information — but do add a new entry when
something changes.

## Scope

### No users or authentication yet
**Chosen:** a single-tenant `todos` table with no `user_id`.
**Rejected:** stub users/auth from the start.
**Why:** auth is the largest chunk of incidental code in a first version.
Adding a nullable-then-backfilled `user_id` later is a cheap migration; the
`uuid` primary key was chosen partly so that step won't force an id rethink.

### Database first, API second, UI third
**Chosen:** ship the schema, migrations, tests, and docs as their own PR with
nothing listening on a port.
**Rejected:** a single PR with table + routes.
**Why:** each slice is reviewable on its own, and the schema is the foundation
every later feature depends on. The HTTP API is
[issue #3](https://github.com/DevAOC/foci-code-challenge/issues/3).

## Tooling

### Prisma over Drizzle or raw SQL
**Chosen:** Prisma 7 with the `pg` driver adapter.
**Rejected:** Drizzle (thinner, SQL-like); raw SQL via `pg` + a migration runner.
**Why:** familiarity, and the planned sorting / filtering / pagination feature
is exactly where Prisma's composable `where` / `orderBy` / `cursor` API saves
the most hand-written (and injection-sensitive) query-building code.

### Local Postgres, no Docker
**Chosen:** Homebrew `postgresql@18` on the developer machine, two databases
(`foci_dev`, `foci_test`) created by hand and documented.
**Rejected:** `docker-compose` with a Postgres service.
**Why:** the application is too small to justify a container runtime as a
prerequisite. Revisit if CI or a second contributor needs reproducibility.

### Real database in tests, never mocked
**Chosen:** Vitest runs against `foci_test`; migrations are applied once by a
global setup and tables are truncated before each test.
**Rejected:** mocking the Prisma client.
**Why:** the schema *is* the thing under test in this slice. Only Postgres can
verify defaults, nullability, `varchar` limits, and `date` round-tripping.

## Schema

### `uuid` primary key, generated client-side
**Chosen:** `id uuid`, generated by Prisma's `uuid()`.
**Rejected:** `bigint GENERATED ALWAYS AS IDENTITY`; database-side
`gen_random_uuid()`.
**Why:** UUIDs are opaque and safe in URLs, and don't leak row counts once ids
are exposed. Client-side generation is Prisma's default and keeps the migration
free of database functions; it can be switched to `dbgenerated(...)` without a
data migration.

### `date` for due dates, not `timestamptz`
**Chosen:** `due_date date NULL`.
**Rejected:** `timestamptz` (time-of-day support).
**Why:** a minimal todo list rarely needs time-of-day, and a bare `date`
sidesteps the most common due-date bug class: off-by-one-day errors from
timezone conversion. A time column can be added later without touching anything.

### `is_completed` boolean only — no `completed_at`
**Chosen:** `is_completed boolean NOT NULL DEFAULT false`.
**Rejected:** adding `completed_at timestamptz` alongside it; replacing the
boolean with `completed_at IS NOT NULL`.
**Why:** two columns describing one fact can disagree
(`is_completed = true, completed_at = NULL`). Keeping only the boolean gives a
single source of truth and matches the original field list. If "completed
today" views are ever needed, add `completed_at` *and* a CHECK constraint
tying the two together at the same time.

### `created_at` and `updated_at`, maintained by the application
**Chosen:** both `timestamptz NOT NULL`; `updated_at` is set by Prisma's
`@updatedAt`.
**Rejected:** a Postgres trigger for `updated_at`; omitting `updated_at`.
**Why:** `updated_at` is cheap now and painful to backfill later. Doing it in
the application keeps the database free of procedural code and keeps the
behaviour visible in the schema file.

### Length limits via `varchar(n)`, not CHECK constraints
**Chosen:** `title varchar(200) NOT NULL`, `description varchar(2000) NULL`.
**Rejected:** `text` columns with `CHECK (length(...) <= n)` and
`CHECK (length(trim(title)) > 0)` written into a hand-edited migration.
**Why:** `varchar(n)` is expressible directly in `schema.prisma`
(`@db.VarChar`), so the migration workflow stays fully generated. The
"non-empty after trim" and "empty description becomes NULL" rules are
validation concerns and belong to the API's zod schemas (issue #3). The limits
are intentionally duplicated between the schema and the validation layer, with
cross-referencing comments.

### snake_case in the database, camelCase in TypeScript
**Chosen:** `@@map("todos")` and `@map("due_date")` etc.
**Rejected:** Prisma's default of quoted mixed-case identifiers (`"Todo"`,
`"dueDate"`).
**Why:** mixed-case identifiers must be quoted in every `psql` session and raw
query; snake_case is the Postgres convention. The model stays idiomatic
TypeScript.

### No `position` column, no indexes
**Chosen:** nothing beyond the primary key.
**Rejected:** a `position` column for drag-reordering; an index on
`(is_completed, due_date)`.
**Why:** manual reordering is the one "small" feature that quietly forces
renumbering logic — it is not a requirement. Indexes are deferred until the
sorting/filtering feature exists and can decide what to index.
